
import 'package:flutter/material.dart';

class NewsSerachView extends StatelessWidget {
  const NewsSerachView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Search News"),
      ),
    );
  }
}
